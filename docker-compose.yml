# docker-compose.yml
version: "3.9"

services:
  backend-1:
    build: .
    working_dir: /app/app  # (From our earlier fix)
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    env_file:
      - backend/.env